import { redirect } from "next/navigation";
import { BookOpenText } from "lucide-react";
import { DiaryEntryForm } from "@/components/diary/diary-entry-form";
import { getCurrentUser } from "@/lib/account/queries";
import { getDiaryEntries } from "@/lib/panel-profits/queries";
export const dynamic = "force-dynamic";
export default async function DiaryPage() { const user=await getCurrentUser(); if(!user) redirect("/sign-in?returnTo=/diary"); const entries=await getDiaryEntries(user.id); return <main className="mx-auto w-full max-w-6xl px-4 py-10 sm:px-6 lg:px-8"><header className="border-b border-slate-800 pb-7"><p className="text-[10px] uppercase tracking-[0.28em] text-violet-300">Player record / broker&apos;s diary</p><h1 className="mt-3 text-4xl text-slate-100">Your professional ledger</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-slate-400">Private notes and player events belong to your account. System-linked events can be added as their contracts become available.</p></header><div className="mt-8 grid gap-8 lg:grid-cols-[360px_1fr]"><DiaryEntryForm/><section><div className="mb-3 flex items-center gap-2"><BookOpenText className="h-4 w-4 text-violet-300"/><h2 className="text-sm uppercase tracking-[0.16em] text-slate-200">Timeline</h2></div><div className="divide-y divide-slate-800 border-y border-slate-800">{entries.map(entry=><article key={entry.id} className="p-5"><div className="flex flex-wrap items-center gap-2 text-[10px] uppercase tracking-[0.16em] text-slate-500"><span>{entry.entry_type}</span><span>·</span><span>{new Date(entry.occurred_at).toLocaleString()}</span></div><h3 className="mt-2 text-base text-slate-100">{entry.title}</h3><p className="mt-2 whitespace-pre-wrap text-sm leading-6 text-slate-400">{entry.body}</p></article>)}{!entries.length&&<p className="p-8 text-sm text-slate-500">No entries yet. Record your first observation.</p>}</div></section></div></main>; }
