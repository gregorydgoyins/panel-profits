import { Award, BookOpenCheck, GraduationCap } from "lucide-react";
import { getLearningCatalog } from "@/lib/panel-profits/queries";
export const dynamic = "force-dynamic";
export default async function LearnPage() { const { classes, certifications, exams, levels } = await getLearningCatalog(); return <main className="mx-auto w-full max-w-7xl px-4 py-10 sm:px-6 lg:px-8"><header className="border-b border-slate-800 pb-7"><p className="text-[10px] uppercase tracking-[0.28em] text-cyan-300">Career systems / learning</p><h1 className="mt-3 text-4xl text-slate-100">Earn authority in the market</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-slate-400">The current Clean learning catalog exposes classes, certifications, exams and career pathway levels. It does not grant credentials from this page.</p></header><div className="mt-8 grid gap-4 lg:grid-cols-3"><section className="border border-slate-800 bg-[#0b0f15] p-5"><GraduationCap className="h-5 w-5 text-cyan-300"/><p className="mt-6 text-[10px] uppercase tracking-[0.2em] text-slate-500">Classes</p><p className="mt-2 text-3xl text-slate-100">{classes.length}</p><p className="mt-2 text-xs text-slate-500">Available learning units</p></section><section className="border border-slate-800 bg-[#0b0f15] p-5"><Award className="h-5 w-5 text-amber-300"/><p className="mt-6 text-[10px] uppercase tracking-[0.2em] text-slate-500">Certifications</p><p className="mt-2 text-3xl text-slate-100">{certifications.length}</p><p className="mt-2 text-xs text-slate-500">Credential definitions</p></section><section className="border border-slate-800 bg-[#0b0f15] p-5"><BookOpenCheck className="h-5 w-5 text-purple-300"/><p className="mt-6 text-[10px] uppercase tracking-[0.2em] text-slate-500">Exams</p><p className="mt-2 text-3xl text-slate-100">{exams.length}</p><p className="mt-2 text-xs text-slate-500">Assessment definitions</p></section></div><section className="mt-8 border border-slate-800"><div className="border-b border-slate-800 p-5"><h2 className="text-xl text-slate-100">Career pathway levels</h2></div><div className="divide-y divide-slate-800">{levels.map((level)=><div key={level.id} className="flex flex-col justify-between gap-2 p-4 sm:flex-row"><span className="text-sm text-slate-200">{level.pathway_name} / Level {level.level}</span><span className="text-xs text-slate-500">Salary cap {level.salary_cap ?? "—"} · Certified bonus {level.certified_bonus_pct ?? "—"}%</span></div>)}</div></section></main>; }
